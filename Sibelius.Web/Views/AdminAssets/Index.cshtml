@model IEnumerable<Sibelius.Web.Models.Asset>

@{
    ViewBag.Title = "Admin Assets";
}

<h2>Admin Assets</h2>

<p>
    <a class="btn btn-primary" href="~/AdminAssets/Create">
        <span class="glyphicon glyphicon-plus"> </span>
    </a>
</p>
<table class="table table-striped table-responsive" >
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.License)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @if (item.License == null)
            {
                <span>N/A</span>
            }
            else
            {
                @item.License.ShortName
            }
        </td>
        <td>
            <a class="btn btn-primary btn-sm" href="~/Assets/Show/@item.Id">
                <span class="glyphicon glyphicon-share"></span>
            </a>
            <a class="btn btn-primary btn-sm" href="~/AdminAssets/Edit/@item.Id">
                <span class="glyphicon glyphicon-pencil"></span>
            </a>
            <a class="btn btn-default  btn-sm" href="~/AdminAssets/Delete/@item.Id">
                <span class="glyphicon glyphicon-trash"></span>
            </a>
        </td>
    </tr>
}

</table>
